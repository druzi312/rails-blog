<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
    	<p><%= link_to tag.name, tag_path(tag) %></p>
    </li>
  <% end %>
</ul>

<%= link_to "<< Back to Articles List", articles_path %> | 

<a href="#">Delete All - Needs Help</a>
